name: Local Variable Practice

on:
  push:
    branches:
      - main

jobs:
  variables:
    runs-on: ubuntu-latest

    steps:
      - name: Create and Print Variable
        run: |
          NAME=Rahim
          echo "Hello $NAME"