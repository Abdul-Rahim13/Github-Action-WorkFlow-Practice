name: Different Triggers Demo

on:
    push:
    
    pull_request:
    
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest
    
        steps:
            - name: Print Message
              run: echo "Workflow triggered successfully!"