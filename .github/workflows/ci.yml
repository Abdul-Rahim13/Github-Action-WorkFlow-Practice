name: Different Triggers Demo

on:
    push:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Print Message
              run: echo "Workflow triggered successfully!"